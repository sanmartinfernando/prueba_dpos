1. para instalar las cosas, requiere tener instalado node
npm install -g @angular/cli
npm install

2. para lanzar la web de desarrollo
ng serve

# comandos angular 
ng serve <--- carga la web para desarrollo
ng build --prod
ng build --prod --aot --output-hashing=all
ng build --aot --output-hashing=all

npx ng build --base-href "/webclient/" <--- Se necesita utilizar el argumento "--base-href" en entornos de integracion ya que la aplicacion se despliega en una subruta.

ng serve -c inte  <--- conectando contra inte

-Componentes:ng generate component my-new-component
–Directivas: ng g directive my-new-directive
–Pipe:ng g pipe my-new-pipe
–Servicios: ng g service my-new-service
–Clases:ng g class my-new-class
–Interface:ng g interface my-new-interface
–Enumerados:ng g enummy-new-enum
–Módulos:ng g module my-module

# INSTALAR DEPENDENCIAS DEL PROYECTO

npm install

# comandos node INICIAR WEB
node main.js

#Borrar cache
npm cache clean --force

#Eliminar cache y reinstalar
PS C:\Repositorios\DPOS_Web_Cliente> Remove-Item -Recurse -Force node_modules
PS C:\Repositorios\DPOS_Web_Cliente> Remove-Item -Force package-lock.json
npm cache clean --force
npm install --legacy-peer-deps

#para corregir codigo estático
ng add @angular-eslint/schematics #para instalarlo en VS Code
ng lint
npx eslint . --ext .ts,.html --fix #corrige problemas de codigo estatico que no requieren intervención manual
npx eslint . --ext .ts,.html #revisa lo que no se puede corregir automaticamente

ng-openapigen #para generar a partir de un yalm