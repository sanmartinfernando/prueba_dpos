/**
 * Constantes de seguridad utilizadas en la aplicación para la gestión
 * de tokens de autenticación en las solicitudes HTTP.
 */
export const SecurityConstants = {
  TOKEN_HEADER_KEY: 'Authorization',
  TOKEN_PREFIX: 'Bearer'
};
