version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build:inte
  artifacts:
    baseDirectory: /dist/diu.pos.webc.angular/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*