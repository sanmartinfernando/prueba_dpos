version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run dev
  artifacts:
    baseDirectory: /dist/diu.mdm.web/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*