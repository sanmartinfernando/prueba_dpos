
version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - |
          if [ "$AWS_BRANCH" == "main" ]; then
            npm run build:prod
          else
            npm run build:inte
          fi
  artifacts:
    baseDirectory: /dist/diu.pos.webc.angular/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*